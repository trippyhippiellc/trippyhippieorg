"use client";

/**
 * PaymentMethodSelector — placeholder component
 * (The actual selector logic is in the main checkout page)
 * This component is exported for organizational purposes.
 */

export function PaymentMethodSelector() {
  return null; // Logic handled in parent component
}
